
// The below is the Context API we have created
import { createContext } from "react"


// The below context contain all the states related to notes
const noteContext = createContext()

export default noteContext